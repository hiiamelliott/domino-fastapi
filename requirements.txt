# FastAPI and async server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# HTTP client for proxying requests
requests>=2.31.0

# Existing dependencies (if not already provided by Domino)
flask>=2.0.0
prometheus-client>=0.18.0
six>=1.16.0

